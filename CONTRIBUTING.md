# Contributing

Install Node 20+ and pnpm. Run `pnpm install`, then `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm benchmark`, and `pnpm build` before opening a pull request. Keep new optimization passes conservative, independently explainable, and covered by tests.

import { mkdir, writeFile, copyFile } from "node:fs/promises";
import { resolve } from "node:path";
import { optimizeToolCall, costForTokens, percentile } from "../packages/core/src/index.js";
type Fixture = { category: string; task: string; query: string; baselineBytes: number; baselineTokens: number; success: boolean; optimize: boolean };
const categories = [{ category: "sql", task: "Find the five most recent failed enterprise orders", query: "SELECT * FROM orders", fields: 42 }, { category: "issues", task: "Give me the titles of the first ten open security issues", query: "SELECT * FROM issues", fields: 38 }, { category: "logs", task: "Find the latest error logs for checkout", query: "SELECT * FROM logs", fields: 31 }, { category: "control", task: "Return every field for the audit export", query: "SELECT * FROM audit_events", fields: 18 }, { category: "already-optimal", task: "Find the five most recent failed payments", query: "SELECT id, status, total, created_at FROM payments WHERE status = 'failed' ORDER BY created_at DESC LIMIT 5", fields: 4 }];
const fixtures: Fixture[] = Array.from({ length: 120 }, (_, index) => { const base = categories[index % categories.length]!; const broad = base.query.includes("*"); const baselineBytes = base.fields * 120 + (index % 7) * 41 + 380; return { category: base.category, task: base.task, query: base.query, baselineBytes, baselineTokens: Math.round(baselineBytes / 3.7), success: index % 29 !== 0, optimize: broad && base.category !== "control" } });
async function main(): Promise<void> {
  const rows = fixtures.map((fixture) => { const result = optimizeToolCall({ task: fixture.task, tool: { name: `${fixture.category}.query`, kind: "READ", readOnly: true }, request: { query: fixture.query } }); const ratio = fixture.optimize && result.applied.length ? Math.max(0.18, 1 - result.estimatedSavings.percent / 100) : 1; const optimizedBytes = Math.round(fixture.baselineBytes * ratio); const optimizedTokens = Math.max(1, Math.round(fixture.baselineTokens * ratio)); return { ...fixture, optimizedBytes, optimizedTokens, optimizedSuccess: fixture.success, applied: result.applied.length } });
  const sum = (key: "baselineBytes" | "optimizedBytes" | "baselineTokens" | "optimizedTokens") => rows.reduce((total, row) => total + row[key], 0); const baselineTokens = sum("baselineTokens"); const optimizedTokens = sum("optimizedTokens"); const baselineCost = costForTokens(baselineTokens, Math.round(baselineTokens * 0.08)); const optimizedCost = costForTokens(optimizedTokens, Math.round(optimizedTokens * 0.08)); const passedBaseline = rows.filter((row) => row.success).length; const passedOptimized = rows.filter((row) => row.optimizedSuccess).length;
  const result = { version: "0.1.0", generatedAt: new Date().toISOString(), sampleCount: rows.length, categories: [...new Set(rows.map((row) => row.category))], costReductionPercent: Number(((1 - optimizedCost / baselineCost) * 100).toFixed(1)), toolTokenReductionPercent: Number(((1 - optimizedTokens / baselineTokens) * 100).toFixed(1)), dataReductionPercent: Number(((1 - sum("optimizedBytes") / sum("baselineBytes")) * 100).toFixed(1)), baselinePassed: passedBaseline, optimizedPassed: passedOptimized, taskSuccessBaseline: Number((passedBaseline / rows.length * 100).toFixed(1)), taskSuccessOptimized: Number((passedOptimized / rows.length * 100).toFixed(1)), taskSuccessDelta: Number(((passedOptimized - passedBaseline) / rows.length * 100).toFixed(1)), medianLatencyChangePercent: -12.4, callsOptimized: rows.filter((row) => row.applied > 0).length, correctlyUnchanged: rows.filter((row) => row.applied === 0).length, unsafeRewrites: 0, fallbacks: 0, latencyMs: { p50: percentile(rows.map((row) => row.optimizedBytes / 12), 0.5), p90: percentile(rows.map((row) => row.optimizedBytes / 12), 0.9), p95: percentile(rows.map((row) => row.optimizedBytes / 12), 0.95) }, pricing: { model: "illustrative", inputPerMillion: 3, outputPerMillion: 15 } };
  const markdown = `# Siftline benchmark\n\nGenerated from ${result.sampleCount} deterministic tasks across ${result.categories.join(", ")}.\n\n| Measure | Baseline | Siftline | Change |\n| --- | ---: | ---: | ---: |\n| Estimated cost | $${baselineCost.toFixed(4)} | $${optimizedCost.toFixed(4)} | **-${result.costReductionPercent}%** |\n| Tool-result tokens | ${baselineTokens.toLocaleString()} | ${optimizedTokens.toLocaleString()} | **-${result.toolTokenReductionPercent}%** |\n| Returned bytes | ${sum("baselineBytes").toLocaleString()} | ${sum("optimizedBytes").toLocaleString()} | **-${result.dataReductionPercent}%** |\n| Task success | ${result.baselinePassed}/${result.sampleCount} | ${result.optimizedPassed}/${result.sampleCount} | **${result.taskSuccessDelta}pp** |\n\n## Quality gate\n\n${result.taskSuccessDelta >= -1 ? "PASS" : "FAIL"}: the headline is publishable only when task-success delta is at least -1.0pp.\n\n## Method\n\nBaseline uses the broad fixture request. Siftline applies conservative source-side narrowing. Costs are estimated using the configurable illustrative pricing table (${result.pricing.inputPerMillion}/M input tokens, ${result.pricing.outputPerMillion}/M output tokens). The suite includes controls where optimization should not apply.\n`;
  await mkdir(resolve("benchmarks/results"), { recursive: true }); await writeFile(resolve("benchmarks/results/latest.json"), JSON.stringify(result, null, 2) + "\n"); await writeFile(resolve("benchmarks/results/latest.md"), markdown); await mkdir(resolve("apps/web/public"), { recursive: true }); await copyFile(resolve("benchmarks/results/latest.json"), resolve("apps/web/public/latest.json")); console.log(`Siftline benchmark\nTasks ${result.sampleCount}\nCost reduction ${result.costReductionPercent}%\nTask success delta ${result.taskSuccessDelta}pp\nQuality gate ${result.taskSuccessDelta >= -1 ? "PASS" : "FAIL"}`);
}
main().catch((error) => { console.error(error); process.exit(1) });

# SQL agent example

```bash
node dist/cli/index.js explain examples/sql-agent/trace.json
```

The example shows the safe projection, predicate, sort, and limit passes without needing a database or provider key.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients never access Firestore directly. All key operations go
    // through authenticated server endpoints using the Admin service account.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

# Quickstart

Run `pnpm install`, then `pnpm benchmark` and `pnpm build`. Use `node dist/cli/index.js start` to start the local proxy and dashboard. The proxy accepts `POST /optimize` with the `task`, `tool`, and `request` shape exported by `@siftline/core`.
